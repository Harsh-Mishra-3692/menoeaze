# ============================================
# MenoEaze Backend Environment Configuration
# ============================================
# Copy this file to .env and fill in your values
# Command: copy env.template .env (Windows) or cp env.template .env (Mac/Linux)

# ============================================
# DATABASE CONFIGURATION
# ============================================
# PostgreSQL Database Connection
# Option 1: Use full DATABASE_URL
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/menoeaze

# Option 2: Use individual components (if DATABASE_URL is not set)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=menoeaze
DB_USER=postgres
DB_PASSWORD=postgres

# ============================================
# JWT AUTHENTICATION
# ============================================
# Secret key for JWT token generation (CHANGE THIS IN PRODUCTION!)
# Generate a secure random string: openssl rand -base64 32
JWT_SECRET=menoeaze-secret-key-change-this-in-production-use-openssl-rand-base64-32
JWT_EXPIRES_IN=7d

# ============================================
# PERPLEXITY AI API (REQUIRED FOR CHAT)
# ============================================
# ⚠️ ADD YOUR PERPLEXITY API KEY HERE ⚠️
# Get your API key from: https://www.perplexity.ai/
# Sign up at https://www.perplexity.ai/ and navigate to API settings
PERPLEXITY_API_KEY=your-perplexity-api-key-here

# ============================================
# OPENAI API (OPTIONAL - for embeddings)
# ============================================
# Only needed if you want to use OpenAI for embeddings
# Leave empty if not using OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4
OPENAI_TEMPERATURE=0.7

# ============================================
# SERVER CONFIGURATION
# ============================================
# Port for backend server
PORT=3001

# Environment: development, production, test
NODE_ENV=development

# CORS origin (frontend URL)
CORS_ORIGIN=http://localhost:3000

# ============================================
# ML SERVICE (OPTIONAL)
# ============================================
# URL for ML microservice (if running separately)
ML_SERVICE_URL=http://localhost:8000
ML_MODEL_PATH=./ml/models/model.pkl

# ============================================
# EMAIL CONFIGURATION (OPTIONAL)
# ============================================
# Only needed if you want to send emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# ============================================
# AWS S3 (OPTIONAL - for file storage)
# ============================================
# Only needed if you want to store files in S3
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
AWS_BUCKET_NAME=

# ============================================
# NOTES
# ============================================
# 1. After creating .env file, make sure to:
#    - Replace PERPLEXITY_API_KEY with your actual key
#    - Update database credentials if different
#    - Change JWT_SECRET to a secure random string
#
# 2. To generate a secure JWT_SECRET:
#    Windows PowerShell: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }))
#    Mac/Linux: openssl rand -base64 32
#
# 3. Never commit .env file to Git (already in .gitignore)

